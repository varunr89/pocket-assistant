<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".PocketAssistantApp"
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:icon="@drawable/ic_launcher_foreground"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@drawable/ic_launcher_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.PocketAssistant">

        <!-- Disable default WM init so Configuration.Provider / explicit init is used. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <uses-native-library
            android:name="libvndksupport.so"
            android:required="false" />
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.PocketAssistant">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".capture.RecordingService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Foreground-gated ML Kit GenAI catch-up transcription. Regular
             (non-foreground-type) service: it only ever runs while the app is
             the top foreground app, so no FGS type is needed. -->
        <service
            android:name=".speech.TranscriptionDrainService"
            android:exported="false" />

        <!-- L1 boot re-arm: schedule-gated capture re-arms at FIRST UNLOCK
             after a reboot (Varun's decision — never before unlock, so NO
             directBootAware). BOOT_COMPLETED is the valid post-first-unlock
             signal for a non-directBootAware receiver: a non-directBootAware
             receiver does NOT receive LOCKED_BOOT_COMPLETED (that broadcast
             requires Direct-Boot awareness), so only BOOT_COMPLETED is
             registered. The receiver does not start the microphone FGS from
             the background (API 34+ while-in-use restriction); it posts a
             resume notification whose tap opens MainActivity, which starts
             the service in the foreground. Protected system broadcast
             (RECEIVE_BOOT_COMPLETED). -->
        <receiver
            android:name=".capture.BootReArmReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- WorkManager FGS: must declare dataSync for setForeground() on API 34+. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:exported="false"
            android:directBootAware="false"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
    </application>
</manifest>
